git init (one time command to initialize the repository)
git status
git add . (replace . with file name if you want specific file to commit)-staged
		(use "git restore --staged <file>..." to unstage)
git commit -m "some comments"

================================================
(one time activity)
git remote add origin https://yourgithubrepository.git

(one time activity)
git branch -M main or master

(everytime you have to do it-- to send changes from local to github)
